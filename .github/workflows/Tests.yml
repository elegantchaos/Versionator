# --------------------------------------------------------------------------------
# This workflow was automatically generated by ActionBuilderTool 2.0.0 (51).
# (see https://github.com/elegantchaos/ActionBuilderPlugin for more details)
# --------------------------------------------------------------------------------

name: Tests

on: [push, pull_request]

jobs:

    macOS-swift60:
        name: macOS (Swift 6.0)
        runs-on: macos-14
        steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Make Logs Directory
          run: mkdir logs
        - name: Select Swift
          uses: swift-actions/setup-swift@next
          with:
            swift-version: "6.0"
        - name: Check Swift Version
          run: swift --version
        - name: Build (release)
          run: swift build --build-tests --configuration release
        - name: Test (release)
          run: swift test --skip-build --configuration release
        - name: Upload Logs
          uses: actions/upload-artifact@v4
          if: always()
          with:
            name: logs
            path: logs

